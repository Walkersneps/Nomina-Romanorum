{
    "//1": "The $PORT:8080 call tells the json file to see if there",
    "//2": "is any value at the 'PORT' environment variable.",
    "//3": "If there is no value there, it will fallback to '8080'",
    "port": "$PORT:8080",
    
    "host": "0.0.0.0",
    
    "//4": "It's very rare that a server manages its own TLS.",
    "//5": "More commonly, vapor is served behind a proxy like nginx.",
    "securityLayer": "none"
}
