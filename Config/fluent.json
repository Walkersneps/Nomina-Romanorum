{
    "//1": "The underlying database technology to use.",
    "//2": "memory: SQLite in-memory DB.",
    "//3": "sqlite: Persisted SQLite DB (configure with sqlite.json)",
    "//4": "Other drivers are available through Vapor providers",
    "//5": "https://github.com/search?q=topic:vapor-provider+topic:database",
    "driver": "memory",
    
    "//6": "Naming convention to use for creating foreign id keys,",
    "//7": "e.g., `user_id`",
    "//8": "`camelCase` option is also available.",
    "keyNamingConvention": "snake_case",
    
    "//9": "Name of the table Fluent uses to track migrations.",
    "//10": "If null, preparations will not be run.",
    "migrationEntityName": "fluent",
    
    "//11": "Character used to join pivot tables, e.g., `user_pet`",
    "pivotNameConnector": "_",
    
    "//12": "If true, foreign keys will automatically be added",
    "//13": "to any `builder.foreignId(...)` calls.",
    "autoForeignKeys": true,
    
    "//14": "Key to specify page number for paginated responses",
    "//15": "e.g., `?page=2` ",
    "defaultPageKey": "page",
    
    "//16": "Default page size if not otherwise specified on models",
    "defaultPageSize": 10,

    "//17": "If true, all queries made to the database will be logged.",
    "log": false
}
